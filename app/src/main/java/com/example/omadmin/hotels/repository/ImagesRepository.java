package com.example.omadmin.hotels.repository;

import android.util.Log;
import android.widget.Toast;

import androidx.annotation.Nullable;
import androidx.lifecycle.LiveData;
import androidx.lifecycle.MutableLiveData;

import com.example.omadmin.hotels.DisplayRoomImages;
import com.example.omadmin.hotels.RoomSingleItem;
import com.example.omadmin.models.HotelRoom;
import com.example.omadmin.models.RoomImages;
import com.example.omadmin.models.Users;
import com.google.firebase.firestore.CollectionReference;
import com.google.firebase.firestore.EventListener;
import com.google.firebase.firestore.FirebaseFirestore;
import com.google.firebase.firestore.FirebaseFirestoreException;
import com.google.firebase.firestore.QueryDocumentSnapshot;
import com.google.firebase.firestore.QuerySnapshot;

import java.util.ArrayList;

public class ImagesRepository {
/*
    HotelRoom hotelRoom=HotelRoom.getInstance();






    Users users=Users.getInstance();
    String hotelId=users.getUid();

    public LiveData<ArrayList<RoomImages>> getRoomImages(String keyword) {
        if (roomImagesLiveData.getValue() == null) {
            fetchItemsFromFirestore(keyword);
        }
        return roomImagesLiveData;
    }
*/

}